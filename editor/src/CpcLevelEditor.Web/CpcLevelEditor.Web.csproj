<Project Sdk="Microsoft.NET.Sdk.Web">
  <!-- ASP.NET Core is in the shared framework, so this needs no package
       either. What it does NOT have is Identity and EF Core, and that is
       deliberate rather than pending - see Program.cs. -->
  <ItemGroup>
    <ProjectReference Include="..\CpcLevelEditor.Application\CpcLevelEditor.Application.csproj" />
  </ItemGroup>
</Project>
